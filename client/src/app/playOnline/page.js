import TicTacOnline from "@components/TicTacOnline";
const page = () => {
  return (
    <div className="flex items-center justify-center h-screen w-full flex-col">
      <TicTacOnline />
    </div>
  );
};

export default page;
